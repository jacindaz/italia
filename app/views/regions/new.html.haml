%h2 Add a Region

.inner-div
  = form_for(@region, url: regions_path) do |f|
    - if @region.errors.any?
      Your region couldn't be saved because:
      %ul
        - @region.errors.messages.each do |field, error|
          %li.errors-colors= "#{field.to_s.capitalize} #{error[0]}"

    .form-group
      = f.label :country_id, "Select a Country"
      = f.collection_select :country_id, Country.all, :id, :english_name

    .form-group
      = f.label "Name in English"
      = f.text_field :english_name, class: "form-control"

    .form-group
      = f.label "Name in Native Language"
      = f.text_field :native_language_name, class: "form-control" 

    .form-group
      = f.label "Description (400 words or fewer)"
      = f.text_area :description, class: "form-control"

    .form-group
      = f.label "Region Website (if any)"
      = f.text_field :region_website, class: "form-control"

    .form-group
      = f.submit "Save", class: "btn btn-default"
